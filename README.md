# xkcd RSS Feed

***

## Intro
[xkcd](http://xkcd.com/) RSS Feed for the CLI (Command Line Interface). Not so great CLI interface for xkcd's RSS feeds.

## Build
To build this application:

`mvn install`

## Usage
`java -jar xkcd-1.X-jar-with-dependencies.jar`